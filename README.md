# Report-8-3


---

# 📋 گزارش پروژه:  LED (Keypad Input + LED Feedback)

## ۱. مقدمه  
در این پروژه، یک **کیپد ماتریسی 4×4** به آردوینو متصل شده و با فشار دادن دکمه‌ها، یک عدد ساخته می‌شود. وقتی کاربر دکمه `*` را فشار می‌دهد، آردوینو بررسی می‌کند که آیا عدد وارد‌شده برابر با `1234` است یا خیر:
- اگر **بله** → LED روشن می‌شود  
- اگر **خیر** → LED خاموش می‌ماند  

همچنین دکمه `#` برای **پاک کردن ورودی** (reset) استفاده می‌شود. این پروژه ترکیبی از **ورودی کیپد**، **پردازش عددی** و **خروجی LED** است.


## ۳. قطعات مورد استفاده  
- برد آردوینو Uno  
- کیپد ماتریسی 4×4  
- یک LED (هر رنگی)  
- یک مقاومت 220 اهم  
- برد برد و سیم‌های جامپر  


## ۴. نحوه‌ی ساخت مدار  
- **کیپد**:  
  - سطرها (Rows) → پین‌های **2, 3, 4, 5**  
  - ستون‌ها (Columns) → پین‌های **6, 7, 8, 9**  
- **LED**:  
  - پایه مثبت → پین دیجیتال **12**  
  - پایه منفی → از طریق مقاومت 220 اهم → GND  
.

## ۵. کد برنامه (اسکچ) 

### 🔹 بخش `setup()`:
```cpp
#include <Keypad.h>

const byte ROWS = 4;
const byte COLS = 4;

char hexaKeys[ROWS][COLS] = {
  {'1','2','3','A'},
  {'4','5','6','B'},
  {'7','8','9','C'},
  {'*','0','#','D'}
};

byte colPins[COLS] = {6, 7, 8, 9};
byte rowPins[ROWS] = {2, 3, 4, 5};

Keypad customKeypad = Keypad(makeKeymap(hexaKeys), rowPins, colPins, ROWS, COLS);

float firstNumber = 0;  // برای ذخیره عدد وارد‌شده

void setup() {
  Serial.begin(9600);
  pinMode(12, OUTPUT);      // تنظیم پین LED
  digitalWrite(12, LOW);    // LED خاموش در ابتدا
}
```

### 🔹 بخش `loop()` — منطق اصلی:
```cpp
void loop() {
  int button = int(customKeypad.getKey()) - 48;  // تبدیل کاراکتر به عدد (e.g., '1'→1)

  if (0 <= button && button <= 10) {  // دکمه‌های عددی 0 تا 9 + '*' (که 42 است، اما در اینجا 10 محدود شده)
    Serial.print(button);               // نمایش عدد در Serial (اختیاری)
    firstNumber = firstNumber * 10 + button;
  }
  else {
    switch (button) {
      case -13:  // '*' → محاسبه/بررسی
        Serial.print(" = ");
        Serial.println(firstNumber);

        if (firstNumber == 1234) {
          digitalWrite(12, HIGH);  // LED روشن
          Serial.println("✅ Correct!");
        } else {
          digitalWrite(12, LOW);   // LED خاموش
          Serial.println("❌ Wrong!");
        }
        firstNumber = 0;  // بازنشانی
        break;

      case -6:   // '#' → پاک کردن
        firstNumber = 0;
        digitalWrite(12, LOW);
        Serial.println("CLEARED");
        break;

      default:
        // دکمه‌های غیرمستخدم (مثل A, B, C, D) — بدون عمل
        break;
    }
  }
}
```
.

## ۶. نحوه‌ی کارکرد (مثال)
1. فشار `1` → `firstNumber = 1`  
2. فشار `2` → `firstNumber = 12`  
3. فشار `3` → `firstNumber = 123`  
4. فشار `4` → `firstNumber = 1234`  
5. فشار `*` → بررسی: `1234 == 1234` → **LED روشن** ✅  
6. فشار `#` → تمامی متغیرها پاک می‌شوند و LED خاموش می‌شود.

## ۷. نتایج  
- LED به درستی بر اساس ورودی کاربر روشن/خاموش می‌شود.  
- دکمه `#` و `*` به درستی عمل می‌کنند.  
- سیستم قادر به ساخت اعداد چند رقمی است.



---
